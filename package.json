{
  "name": "jira_clone",
  "version": "1.0.0",
  "author": "Ivor Reic",
  "license": "MIT",
  "scripts": {
    "pre-commit": "cd api && yarn run pre-commit && cd ../client && yarn run pre-commit",
    "install-dependencies": "yarn install && cd api && yarn install && cd ../client && yarn install",
    "build": "cd api && yarn run build && cd ../client && yarn run build",
    "start:production": "cd api && yarn run start:production && cd ../client && yarn run start:production"
  },
  "devDependencies": {
    "husky": "^4.0.0-beta.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn run pre-commit"
    }
  },
  "dependencies": {}
}
